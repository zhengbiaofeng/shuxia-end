/**
 * 布局原语统一出口
 *
 * 推荐用法:
 *   import { ResponsiveStage, AppShell, PageHeader, CardSurface } from '@/components/layout'
 */

// 暂时使用相对路径，避免引入 @ 别名配置
export { default as ResponsiveStage } from './ResponsiveStage.vue'
export { default as AppShell } from './AppShell.vue'
export { default as PageHeader } from './PageHeader.vue'
export { default as CardSurface } from './CardSurface.vue'
